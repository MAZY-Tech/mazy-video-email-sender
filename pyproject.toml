[project]
name = "mazy-video-tools-notification"
version = "1.0.0"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = "src"
addopts = "--cov=src --cov-report=term --cov-report=xml"
testpaths = ["tests"]